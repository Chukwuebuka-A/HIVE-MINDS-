name: PHP CI Check

on: [push]

jobs:
  php-check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5

      - name: Check PHP syntax
        run: |
          for f in $(git ls-files '*.php'); do
            php -l "$f" || exit 1
          done
